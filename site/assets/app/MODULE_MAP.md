# app.js split module map

This file records the first split of `site/assets/app.js`. It is meant to keep later refactors honest: module state moves with its functions, high-level callbacks use action injection, and the entry module wires everything before calling `init()`.

| Module | Main exports | Module state / closure state | Direct imports | Action injection |
| --- | --- | --- | --- | --- |
| `codex-route-compat.js` | `normalizeRoutePath`, `normalizeCodexRoutePath` | permanent merged-book path prefixes | none | none; app route loading/history replay and recent/resume flows preserve the source book ID until tree-validated path migration finishes |
| `../data-source.js` | `initializeDataSource`, `fetchDataJson`, `fetchDataJsonBatch`, `fetchDataJsonResult`, `getDataSource` | active R2/proxy/local release context + one initialization promise | none | none; shared by atlas and community. Official host reads the immutable R2 release selected by `current.json`; Pages previews and public-R2 failures read that same release through the same-origin `/data` Function proxy; localhost/local editions read the ignored local `site/data/`. All paths resolve against `document.baseURI`, so sub-path local deployments work. Bootstrap files load as one atomic batch: a failure of `codexes.json`/`media.json` discards every in-flight public-R2 result and reloads the whole batch through the exact same release proxy, so gating never mixes releases |
| `../app.js` | `init`, `loadCodex`, `openFavoritesView`, `openSiteSearchView`, `applySearch`, `applyFilter` | `codexLoadSeq` | all feature modules | injects router, codex UI, masonry, history, favorites, Tag relay, UI actions before `init()` |
| `state.js` | constants (including separate adult-confirmation and NSFW-display storage keys), `state`, `normalizeDensity`, `densityConfig`, `normalizeSearchScope` | shared `state` object | none | none |
| `utils.js` | `$`, `safeJsonParse`, `safeHttpUrl`, path helpers, `prefersReducedMotion`, `updateSearchClear`, `updateScrollProgress`, `clamp`, `esc` | none | none | none |
| `modal.js` | `openMask`, `closeMask`, `registerMaskHistory`, `trapFocus`, focus helpers | `maskTimers`, `maskOpeners` | `utils.js`, `browser-history.js` | none |
| `browser-history.js` | `configureBrowserHistory`, `initializeBrowserHistory`, `persistedHistoryState`, `commitHistoryRoute`, `beginLayeredSearch`, layer registry (`registerHistoryLayer`/`openHistoryLayer`/`closeHistoryLayer`/`forgetHistoryLayer`/`topHistoryLayerId`), `goBackFrom`/`canGoBackFrom`, scroll checkpoints, restore-token helpers | `config`, `initialized`, `restoring`, `pendingBack`, `restoreToken`, `currentEntry`, `reloadCollapse`, `deferredLayerOpens`, `scrollTimer`, `layerRegistry` | none | page adapter injected via `configureBrowserHistory` (`captureRoute`, `urlForRoute`, `applyRoute`, `restoreScroll`, `isEmptySearchRoute`); shared by atlas (`router.js`) and community (`../community/router.js`) |
| `feedback.js` | `setLoading`, `showSkeleton`, `hideSkeleton`, `toast` (optional accessible action button) | toast/action timing, skeleton timing state | `utils.js` | none |
| `feedback-progress.js` | public/admin shared feedback progress labels, descriptions, closed-state helpers, and the public-card 5-step flow model (`FEEDBACK_PROGRESS_FLOW`, `feedbackProgressFlow`) | none | none | none |
| `clipboard.js` | `writeClipboardText` with truthful Clipboard API / `execCommand` / manual outcomes | none | none | UI-free capability layer shared by atlas, feedback, favorites backup, and community |
| `clipboard-fallback.js` | `showClipboardFallback`, `closeClipboardFallback`, `scrubClipboardFallback` | lazily-created manual-copy dialog | `modal.js` | `scrubClipboardFallback` 在撤销分级权限时把 textarea 清空并关面板——关闭动画期间也不能把原文继续留在 DOM / 可访问树里 |
| `nai-sd.js` | `naiToSd`, `fmtSdWeight`, `formatCopyText` | none | none | dependency-free conversion shared by atlas and community |
| `tag-relay-core.js` | versioned relay state load/save/normalize, stable entry keys, `touchInboxEntry` intake, plan CRUD, item ordering, NAI/SD/plain compilation with dedupe reporting (`merged`/`mergedTotal`), bounded copy history, `weightAppliesTo`(纯文本不带权重语法，界面据此置灰), `trimStateToBudget`(按字节从最旧的 history 起裁，供 store 撞配额时自救) | none | `nai-sd.js` | 纯计算 + 可序列化结构，无 DOM。`accessKnown` 是**黏性**的（显式带了就尊重，没带才由 `hasAccessEvidence` 推断）——否则 fail-closed 保护会被自己的序列化解除；`restoreHistoryAsPlan` 收一个可选的 `isLocked` 谓词，把分级把关下沉到这里，而不是只靠视图层一个调用点维持不变式。**inbox 的规范顺序是新的在前（schema v2）**，v1 数据在 `normalizeRelayState` 里一次性掉头；`touchInboxEntry` 是「复制即入库」的唯一入口（撞 key 时置顶并刷新快照，满 `TAG_RELAY_INBOX_LIMIT`=50 从尾部裁掉） |
| `tag-relay-snapshot.js` | `sourceContext`, `relayKeyFor`, `snapshotEntry`, `snapshotLocked` | none | `state.js`, `access.js`, `data.js`, `media.js`, `tag-relay-core.js` | 活词条 → 可序列化快照的适配层。归属按 `_srcCodexId` 回溯真实法典；分级随快照冻住，日后关掉开关也能锁住。⚠ `snapshotLocked` 只看内存里的 `state.allowNsfw`，**不要读 localStorage**——`ui.js` 是在会话内改它的，同标签页的写入不触发 `storage` |
| `tag-relay-store.js` | `relayState`, `relayInbox`, `activeRelayPlan`, `commitRelay`, `prepareCopiedEntry`, `recordPreparedCopiedEntry`, `recordCopiedEntry`, `subscribeRelay`, `setupRelayStore` | 唯一的内存副本 `current` + 订阅表 | `feedback.js`, `tag-relay-core.js`, `tag-relay-snapshot.js` | **状态的唯一所有者**，且**不 import 任何视图模块**——这条约束就是单一所有者的结构保证。`commitRelay(mutator, {changed})` **是 async**，且是唯一写入口（`navigator.locks` 真互斥量排队，拿锁后重读磁盘，先落盘再换内存，配额失败时内存不失步；撞配额会自动裁掉最旧的复制历史再重试一次）。⚠ 调用方必须 `await`——忘了会拿到 Promise，而 Promise 是 truthy 的，`if (action.ok)` 会静默走错分支。没有 `navigator.locks` 的老浏览器退回原来那把 localStorage 短锁（只能降低概率、**不是**事务），改完广播；跨标签页 `storage`（含 clear）与 bfcache `pageshow` 也收在这里 |
| `tag-relay-action.js` | `setupRelayAction`, `requestRelayAction`, `cancelRelayAction` | 当前内联操作与焦点返回点 | none | 侧栏内的轻量命名 / 确认条，替代中转站里的浏览器原生 `prompt` / `confirm`，不另叠模态框打断编排。 |
| `tag-relay-rail.js` | `setupTagRelayRail`, `openRelayRail`/`closeRelayRail`/`toggleRelayRail`, `isRelayRailModal`, `showRailTab`, `railPaneRoot`, `setRailPaneRenderers`, `markRailDirty` | `rail`/`activeTab`/`dirty` | `browser-history.js`, `modal.js` | 侧栏外壳。**素材与编排同屏，没有页签**（2026-08-20 一屏化）——`flush()` 脏了哪块画哪块，`showRailTab` 退化成「把某个分区滚进视野」。三态：**停靠（>1240px，走 `.layout` 的 flex 挤瀑布流，不是浮层）/ 抽屉（600~1240px）/ 底部 sheet（≤600px）**。⚠ 停靠态不进 Esc 链、不算 `overlayOpen`、不注册历史层——它是页面家具，与 `.sidebar` 同性质；断点必须与 `tag-relay.css` 的 `@media` 对齐。渲染器由 `setRailPaneRenderers` 注入，外壳不认识分区模块，所以分区能反过来 import 它而不成环 |
| `tag-relay-compose.js` | `setupRelayCompose`, `renderCompose`, `renderComposeCounters`, `refreshComposeAccess`, `addSourceToPlan`, `closeInspector` | `refs`/`selectedItemId`/`outputFormat`/`joinMode` | `feedback.js`, `copy.js`, `tag-relay-action.js`, `tag-relay-core.js`, `tag-relay-snapshot.js`, `tag-relay-store.js` | 侧栏「编排」分区（一屏化后在**上半屏**）：块轨道用**带图大卡**（一行两个，缩略图 + 标题 + 右上角序号；块数天然很少，做小反而难点难拖），点一下只选中并在分区头就地出 ↑↓●✎× 工具条，双击或 ✎ 才开编辑器；方案条并进栏头，块编辑器与复制历史都是**浮层**不占常驻高度；成品默认收成一行摘要（点开才展开两个文本框），且**紧挨编排区**而不是栏底——复制的是这个方案，中间不该隔着素材货架。⚠ 卡片主体是 `div[role=button]` 不是 `<button>`：Chrome 里按钮会吞掉拖拽手势，draggable 的祖先收不到 dragstart（编排与输出合成一屏，避免每复制一次就切页）。素材靠显式按钮加入；HTML5 拖拽只负责本页签内的块排序。⚠ 复制成品走 `copyText` 但传 `convert:false`（格式已由栏内 NAI/SD/纯文本 决定，再按全局 SD 开关转一次就是二次转换），且**不传 `entry`**——成品不能回流进「最近复制」 |
| `tag-relay.js` | `setupTagRelay` | `warehouseRoot`/`sourceMode`/`favorites` | 上面全部 + `access.js`, `data.js`, `fav-codex.js`, `favorites-backup.js`, `media.js` | 接线层：浮钮计数 + 「素材」分区（一屏化后在**下半屏**，最近复制 / 收藏两列）。素材也用芯片，**单击即加入方案**，也可以直接拖进上方轨道；把方案卡拖回素材区 = 移出方案（往上是加、往下是拿出）。拖拽载荷用带类型的 MIME（`RELAY_PLAN_MIME` / `RELAY_SOURCE_MIME`）而不是跨模块共享变量，素材载荷里带整条快照——收藏来源的条目不在 `relayInbox` 里，按 key 回查会落空——原先一条 100px 里有 28px 是常驻按钮、42px 是缩略图，一屏只看得到 4 条。⚠ 收藏列**不得**照搬独立页那版往共享 `state` 写 codexes/favs/media 的引导代码——主站这些是活的，写进去会冲掉正在跑的索引。点星标同页不发事件，所以每次切到收藏页签重建 |
| `sd-mode.js` | `readSdMode`, `writeSdMode`, `SD_MODE_STORAGE_KEY` | none | none | shared localStorage contract; community mirrors the atlas switch without importing atlas state |
| `local-ownership.js` | `readOwnedRecords`, `rememberOwnedRecord`, `ownedRecordIds` | none | none | bounded/expiring local ownership markers used by feedback and community submissions |
| `access.js` | `isNsfwCodex`, `entryRating`, `isNsfwRating`, `isNsfwPathSegment`, `isEntryNsfw`, `isCodexLocked`, `firstUnlockedCodex`, `showNsfwLockedHint`, `isR18gName`, `isR18gEntry`, `isR18gPath`, `isR18gBlocked`, `isEntryAccessBlocked`, `showR18gLockedHint` | none | `state.js`, `feedback.js` | none |
| `data.js` | atomic bootstrap, codex loading, normalization, tree build, R2/static status helpers | none | `state.js`, `utils.js`, `media.js`, `feedback.js`, `../data-source.js` | none |
| `search.js` | query parse, phrase-aware matching/highlighting, `invalidateSearchableText` | per-entry `searchableTextCache` (`WeakMap`) | `state.js`, `utils.js`, `media.js`, `favorites.js` | none |
| `router.js` | `setRouterActions`, `readUrlState`, `syncUrlState`, `openEntryDeepLink`, `captureAtlasRoute`, `atlasUrlForRoute`, `configureAtlasHistory`, `initializeAtlasHistory`, `beginAtlasLayeredSearch` | `routerActions` | `state.js`, `utils.js`, `media.js`, `access.js`, `feedback.js`, `browser-history.js` | `onUrlSync`, `renderTree`, `applyFilter`, `openLightbox`, `updateVirtualCards`, `applyHistoryRoute`, `restoreHistoryScroll` |
| `media.js` | image availability, asset paths, rev/cache bust URLs | none | `state.js`, `utils.js` | none |
| `copy.js` | `copyEntry`, `copyText`, combined-prompt building, positive→negative relay, and copy side effects（含分级守卫：`options.entry` 冻结来源快照并入库，`options.accessEntry` 只冻结分级不入库；快照建不出来时走 `accessProbe`/`probeLocked` 实时复核，**判不出来一律锁定**，不再因 `null` 而 fail-open） | none | `state.js`, `feedback.js`, `history.js`, `clipboard.js`, `clipboard-fallback.js`, `nai-sd.js`, `copy-fx.js` | none |
| `favorites.js` | `setFavoritesActions`, `favKey`, `favKeys`, `isFav`, `toggleFav`, `saveFavs` | `favoriteActions`; codex alias lookup memo keyed by `state.codexes` array identity | `state.js`, `feedback.js`, `data.js`, `favorites-backup-core.js` | `applyFilter`, `refreshFavoritesView` |
| `favorites-backup-core.js` | versioned backup schema, alias/entry-owner normalization, key variants, merge/replace planning, two-key commit/rollback | permanent favorite-owner migration table | none | none |
| `favorites-transfer.js` | size-bounded gzip/base64url text transfer encode/decode | none | none | browser-native `CompressionStream` / `DecompressionStream`, with raw JSON fallback |
| `favorites-backup.js` | `setupFavoritesBackup`, `subscribeFavoritesChanges` | dialog state, codex index promise | `favorites-backup-core.js`, `favorites-origin-migration.js`, `favorites-transfer.js`, `clipboard.js`, `clipboard-fallback.js`, `modal.js`, `browser-history.js` | page-provided codex index; emits scoped favorite-change events |
| `favorites-origin-migration.js` | `setupFavoritesOriginMigration`, `createFavoritesMigrationRestore`, `buildFavoritesMigrationUrl`, `isTrustedFavoritesMigrationEvent`, nonce/marker/banner helpers, migration constants (`FAVORITES_MIGRATION_*`) | none (marker/banner state lives in localStorage) | `favorites-backup-core.js` | none; consumed by `favorites-backup.js` and the standalone rescue page `/_favorites-migration-202607.html` (postMessage bridge from old `pages.dev` origin, see decisions/旧Pages域收藏迁移桥.md) |
| `fav-codex.js` | `FAVORITES_CODEX_ID`, `buildFavoritesCodex` | none | `state.js`, `data.js`, `access.js`, `media.js`, `favorites-backup-core.js` | none |
| `site-search.js` | `SITE_SEARCH_CODEX_ID`, `buildSiteSearchCodex`, `invalidateSiteSearchCodex` | full-success memo + in-flight build, keyed by codex-index identity and access flags; generation invalidation | `state.js`, `data.js`, `access.js`, `media.js` | none |
| `report.js` | `setupReport`, `openReportDialog`, `feedbackTimeoutSignal`, feedback submission/public progress UI and context packing | `currentPayload`, `currentTrigger`, public feedback list/filter state | `state.js`, `utils.js`, `feedback.js`, `feedback-progress.js`, `modal.js`, `media.js`, `original-capability.js`, `local-ownership.js`, `clipboard.js`, `clipboard-fallback.js` | none |
| `announcements.js` | `setupAnnouncements`, `loadAnnouncements`, `openAnnouncementsPanel`, badge update | `announcements`, loading flags | `utils.js`, `modal.js`, `history.js` | none |
| `onboarding.js` | `setupOnboarding`, `maybeShowOnboarding` | `initialRouteUrl`, `step`, `prompted` | `utils.js`, `modal.js` | none |
| `resume-prompt.js` | `isDefaultResumeRoute`, `shouldOfferResume`, `setupResumePrompt` | none | `state.js`, `history.js` | one-session prompt plus explicit permanent opt-out; delegates restoration to history |
| `intro.js` | `MOTION_STORAGE_KEY`, `introMode`, `isIntroArmed`, `startIntro`, `beginIntroReveal`, `markIntroDataReady`, `introSettled`, `finishIntro` | settle/dataReady promises, asset-ready gate, timer list, counter rAF, skip-listener flag | `utils.js` | 「显影」opening adapted from the 动效方案演示 `INTROS.diffusion`: noise veil → typewriter `masterpiece, best quality, 法典图鉴` → truthful `step N/28` data gate → **continuous** responsive de-blur (real photos deliberately do not use the prototype's `steps(2)`). The banner remains one small focus; masonry adds a short image-only wave to at most 1 mobile / 3 desktop decoded focus images, while card shells stay on opacity/translate. **Always plays** except `prefers-reduced-motion` / `motion=off`. `index.html` arms before first paint and owns the 8s timeout event; `app.js` starts the loader, calls `beginIntroReveal()` before initial DOM creation, then `markIntroDataReady()` after render and waits for `introSettled()`. Four-state machine `intro-arm → +intro-run → +intro-reveal → intro-done`; `intro-done` suppresses only static shell replays, while current dynamic nodes receive `intro-no-replay` so later codex switches still animate. Emits `intro:reveal` / `intro:settle` (with `skipped`) for masonry hold/release and skip cleanup. |
| `copy-fx.js` | `playCopySample` | singleton chip/noise nodes, timer list, last-copy timestamp, last pointer position | none | copy signature 「采样」, ported from `COPY_FX.diffusion`: image-plane noise flash (180ms) + card sampling ring + a seed chip whose glyphs scramble into `✓ 已复制正面 · N tags` (label passed per call so the negative-prompt follow-up says 已复制负面). Called by `copy.js` **only after `clipboard.writeText` resolves ok**. The chip lives on `<body>` with `position:fixed` — inside a card it would be clipped by `.masonry`'s `contain:layout paint`. 真正收入中转站时改成更高层级的实色芯片与 500ms 抛物线：栏关闭飞最上方浮钮，栏打开飞「最近复制」页签，落点再脉冲确认；另有扫过 `.card-tags` 的掌灯 sweep（`::after`，**不是**注入节点——masonry 要量它的 `scrollHeight`）。Repeat copies within 600ms drop to a simplified version. ⚠ 每轮用 `chipGen` 令旧回调失效，WAAPI 句柄主动 cancel，后台标签页另有定时器兜底收尾；不能把旧动画终态留给下一轮复用。 |
| `home-shortcut.js` | platform/WebView copy helpers and `setupHomeShortcutGuide` | lazily-created guide dialog | `modal.js`, `utils.js` | touch-primary shortcut instructions; embedded WeChat/QQ opens the system browser first; deliberately no service worker/offline promise |
| `codex-ui.js` | codex picker/tree/banner/archive/result/empty/random UI; `syncCodexPickerCounts`, `invalidateAccessViewMemo` | `codexUiActions`, `tipTimer`, `tipIndex`, `EXT_ICON`; access-tree/hidden-count memo keyed by entries/empty-path identities and access flags | `state.js`, `utils.js`, `access.js`, `data.js`, `media.js`, `feedback.js`, `browser-history.js` | `loadCodex`, `applySearch`, `applyFilter`, `openLightbox`, `syncUrlState`, `updateVirtualCards` |
| `masonry.js` | virtual masonry render, card creation, image setup, relayout/density anchors, 显影 card entry (`ENTRY_WAVES` intro/scroll waves + fling gate + `intro:reveal`/`intro:settle` hold-and-release) | `masonryActions`, `virtualRaf`, `relayoutTimer`, `relayoutAnimTimer`, `relayoutQueuedAnimate`, `relayoutAnimating`, `lastRelayoutAt`, `introHeld`, fling sample | `state.js`, `utils.js`, `feedback.js`, `search.js`, `media.js`, `copy.js`, `favorites.js`, `codex-ui.js` | `openLightbox`, `copyEntry`, `toggleFav`, `isStaged`, `toggleStage`, `reportEntry` |
| `lightbox.js` | FLIP helpers, `openLightbox`, `closeLightbox`, cross-entry `stepLightbox`, original/share/favorite controls, `renderLightbox`, `bindLightboxControls` | `lbSeq`, `lbCloseTimer`, `lbSourceImg`, `lbFocusReturn`, thumbnail reuse identities, `lbPreloadCache` | `state.js`, `utils.js`, `data.js`, `original-capability.js`, `masonry.js`, `search.js`, `copy.js`, `nai-sd.js`, `history.js`, `router.js`, `media.js`, `access.js`, `report.js`, `browser-history.js`, `favorites.js` | none |
| `original-capability.js` | resolves real source codex and gates user-visible originals by codex + image capability | none | `state.js`, `data.js`, `media.js` | none |
| `history.js` | `setHistoryActions`, recent entries, browse state save/resume/open, `checkpointBrowseState`, `restoreBrowseScroll` | `historyActions`, `browseSaveTimer`, browse-save suppression, `browseScrollRestoreSeq` | `state.js`, `utils.js`, `media.js`, `router.js`, `codex-route-compat.js`, `access.js`, `feedback.js`, `data.js`, `fav-codex.js`, `site-search.js`, `browser-history.js` | `loadCodex`, `openFavoritesView`, `openSiteSearchView`, `openEntryDeepLink`, `renderTree`, `applyFilter`, `updateVirtualCards` |
| `ui.js` | `setUiActions`, density/update-filter orchestration, global UI binding | `uiActions`, `THEME_ICONS` | `state.js`, `utils.js`, `feedback.js`, `access.js`, `codex-ui.js`, `masonry.js`, `router.js`, `history.js`, `lightbox.js`, `modal.js`, `announcements.js`, `report.js`, `onboarding.js`, `home-shortcut.js`, `browser-history.js` | `loadCodex`, `openFavoritesView`, `openSiteSearchView`, `exitSiteSearchView`, `applySearch`, `applyFilter` |
| `edit.js` | `initEditMode` | `caps`, `acts`, `enabled`, `saving`, `panelEntryId`, `treeAddBtn` | `state.js`, `utils.js`, `feedback.js`, `lightbox.js`, `codex-ui.js`, `search.js`, `site-search.js`, `edit-core.js` | receives `{loadCodex, applyFilter}` from `app.js`; registers `decorateDoor` via `setCodexUiActions` (edit mode turns the picker's community door into the codex manager). **Dynamic-import only** — `app.js` loads it (`maybeLoadEditMode`) only when the local edit server answers `/__edit__/ping`; production never loads it. Owns the whole edit-mode UI (pencil toggle, lightbox edit panel, result-bar ＋新增词条, tree row ⋯ menu, codex manager, image set/delete). Card clicks are intercepted with a capture-phase delegate on `#masonry` (open lightbox instead of copy), so it needs no masonry hook |
| `edit-core.js` | `TREE_PATH_SEP`, `splitTreePath`, `joinTreePath`, `buildPathList`, `diffFields`, `validateEntryForm`, `mergeEntryInPlace` | none | none | none; pure functions consumed by `edit.js` and node-tested by `tools/test_edit_client.mjs`. `TREE_PATH_SEP` mirrors codex-ui's `dataset.path` separator (U+0001, always written as `\u0001`) |

## Wiring notes

- `app.js` imports modules statically, calls all `set*Actions(...)`, then calls `init()`.
- `router.js` does not import `history.js`; `syncUrlState()` calls injected `onUrlSync`.
- `router.js`, `history.js`, and `codex-ui.js` call masonry relayout through injected `updateVirtualCards`; they must not statically import `masonry.js` for that edge.
- `masonry.js` does not import lightbox or UI orchestration; card behavior is injected with `setMasonryActions(...)`.
- 中转站按 core / store / view 三层拆开：`tag-relay-core.js`（纯计算）→ `tag-relay-snapshot.js` + `tag-relay-store.js`（快照与持久化）→ `tag-relay-rail.js` + `tag-relay-compose.js` + `tag-relay.js`（DOM）。入库不再有手动按钮，改由 `copy.js` 在等待剪贴板前经 `options.entry` 冻结来源快照、写入成功后再记录（opt-in：不传就不入库，分享链接与非词条复制天然免疫）；raw tag 这类不入库却仍归属词条的复制用 `options.accessEntry` 冻结并复核分级。独立页 `tag-relay.html` 与 `tag-relay-workbench.js` 已删除，旧地址在 `_redirects` 里 301 回首页。
- `masonry.js` calls feedback through injected `reportEntry`; `report.js` owns submission, context packing, fallback copy, and must not import masonry.
- `modal.js` owns shared mask/focus behavior; UI modules may import it directly instead of creating per-dialog focus traps.
- `access.js` owns every access gate, codex-level and entry-level: `isCodexLocked`/`firstUnlockedCodex` gate whole books, while the entry-level rating gate (`entryRating` reads a single entry's `rating` field, `isNsfwRating`/`isEntryNsfw` fold in NSFW path segments, `isEntryAccessBlocked`/`isR18gBlocked` add the R18G name/path checks) hides or locks individual entries and tree nodes. Consumed by `codex-ui.js`, `router.js`, `lightbox.js`, `history.js`, `ui.js`, `fav-codex.js`, `site-search.js`.
- `browser-history.js` owns the managed history stack (route records + overlay layers) and is page-agnostic: atlas adapts it in `router.js`, community in `../community/router.js`. Overlay handlers registered with `registerHistoryLayer` must be direct open/close functions that never touch history; the user-facing open/close wrappers decide push/replace/back. Route records carry `parentId`/`transition`/`scrollY`; **every** managed `popstate`, including pure-layer/reload-collapse early returns, invalidates the previous restore token and unlocks history writes. A layer the user opens during an async restore is preserved visually and registered only after that restore finishes if it is still open. `initializeBrowserHistory` re-adopts a managed record persisted in `history.state` so reload/cross-document return keep identity and scroll. If reload intentionally drops transient UI (layers or a community detail), initialization folds that child chain back to the nearest visible parent before user input, carrying the restored route/scroll without replaying it.
- `data.js` stores the **in-flight Promise** in `state.codexCache` before downloading/normalizing a codex. Concurrent callers share it; rejection and external-source fallback remove that exact promise so a later call can retry.
- `favorites.js` owns favorite keys and persistence; it only asks the UI layer to re-filter via injected `applyFilter`. Keys always belong to an entry's REAL codex: entries from the favorites view carry `_srcCodexId` and are resolved through `findCodexMeta`. Alias lookup is memoized by the `state.codexes` array reference, so edit-mode index reloads invalidate it naturally.
- `favorites-backup-core.js` is browser-agnostic: it owns the public V1 JSON contract, validation, alias canonicalization, deterministic serialization, restore planning, and rollback-safe writes to both favorite storage keys. `favorites-backup.js` owns the shared dialog/file flow and emits only scope metadata; `app.js` and `community.js` re-read their own in-memory favorite state after same-page or cross-tab changes.
- `fav-codex.js` builds the temporary "全部收藏" view data (id `favorites`, not registered in `state.codexes`): it merges favorited entries from all codices via `fetchCodex` (cached), resolves cross-codex image paths (codex-mode sources get `assetCodexId`, relative-mode sources get pre-resolved absolute URLs), prefixes `path` with the source codex name (tree groups by source), and tags entries with `_srcCodexId`/`_srcCodexTitle`/`_srcType`/`_srcPath`. `app.js` enters this view through `openFavoritesView()` while keeping `state.browseCodex` as the real selected codex; URL sync writes the real `codex` plus `fav=1`, and the topbar favorite toggle is the user-facing entry/exit. Consumers of `_src*`: `favorites.js` (keys), `history.js` (recent entries), `report.js` (feedback context), `masonry.js` (pack behavior).
- `site-search.js` builds the temporary "全站搜索" view data (id `site-search`, not registered in `state.codexes`): it merges entries from all currently unlocked codices via `fetchCodex` (cached), resolves cross-codex image paths with the same rules as `fav-codex.js`, prefixes `path` with the source codex name, and tags entries with `_src*` source metadata. Only a fully successful, non-fallback build is memoized; failed/degraded builds retry next time, while edit mode explicitly invalidates both this memo and the per-entry search/access caches after in-place changes. `app.js` enters this view through `openSiteSearchView()` only when `state.searchScope === "site"` and `state.query` is non-empty, and rechecks that intent after the async build and immediately before the View Transition callback. Clearing search or switching scope back to `codex` exits to `state.browseCodex`. URL sync writes the real `codex` plus `q` and `scope=site`; old `q` links without `scope` remain current-codex search.
- `copy.js` currently records copy side effects through `history.js`; `history.js` must not import `copy.js` unless that side effect is first moved to the orchestration layer.
- `ui.js` keeps `applyDensity()` as orchestration: state/localStorage/body class, control state, masonry anchor and relayout, and toast.
- `edit.js` / `edit-core.js` are the local editor (P0, see decisions/本地法典编辑器.md). They are **dynamic-import only**: `app.js maybeLoadEditMode()` probes the same-origin `/__edit__/ping` after first render (including the empty-index state; guarded by `isLocalOrigin()` + `requestIdleCallback` with a timeout because the masonry page is rarely idle) and imports `edit.js` only when the local `tools/edit_server.py` answers. Production and plain preview return 404, so the editor never loads and adds zero weight. Main-code touch-points are kept tiny and inert when the editor is absent: the probe block in `app.js`, the `lightbox:rendered` and `codex:loaded` CustomEvents, the opt-in `openLightbox(..., {allowEmpty:true})` placeholder used only to edit no-image entries, the `codexUiActions.decorateDoor?.()` hook on the picker's community door, the `emptyCategories` awareness in `buildAccessTree()` (only fires for books that have the field, i.e. ones the editor touched), and the exported `closeCodexPicker()`. `edit.js` reaches the rest through injected `{loadCodex, applyFilter, captureRoute}` and by importing `lightbox.js`/`codex-ui.js`/`modal.js` directly (same shared instances). Its dynamic dialogs use the shared modal history/focus infrastructure. It never edits virtual-view entries (`_srcCodexId`) or external-source books.
